const senderStyle = theme => ({
    root: {
        width: '100%',
        height: '100%',
        width: 900,
        borderRadius: 5,
        padding:10,
        backgroundColor: theme.palette.background.paper
    },
    inline: {
        display: 'inline',
    },
})

export default senderStyle;