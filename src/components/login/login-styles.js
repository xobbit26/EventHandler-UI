const loginStyles = theme => ({
    root: {
        width: '100%',
        height: '100%',
        width: 900,
        borderRadius: 5,
        padding: 10,
        backgroundColor: theme.palette.background.paper
    }
});

export default loginStyles;